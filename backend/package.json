{
  "name": "propman-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix ../frontend install && npm --prefix ../frontend run build",
    "dev": "node --watch src/index.js",
    "start": "npm run build && node src/index.js"
  },
  "type": "module",
  "dependencies": {
    "better-sqlite3": "^12.10.1",
    "cors": "^2.8.5",
    "express": "^4.21.0"
  }
}
